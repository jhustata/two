use transplants, clear
export delimited using transplants.txt, delimiter(tab) replace
use transplants_hw, clear
export delimited using transplants_hw.txt, delimiter(tab) replace 
use donors, clear
export delimited using donors.txt, delimiter(tab) replace 
use donors_recipients, clear
export delimited using donors_recipients.txt, delimiter(tab) replace 
use tx_yr, clear
export delimited using tx_yr.txt, delimiter(tab) replace 
